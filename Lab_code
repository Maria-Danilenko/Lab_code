#include <iostream>
#include <iomanip>
#include "windows.h"
#include "math.h"

using namespace std;

int main()
{
	double x = 3, y;

	while (x <= 6)
	{
		y = cos(pow(x, 2)) + pow(sin(x), 2) + 3;
		cout << setw(10) << "\nY = " << y << setw(10);
		x = x + 0.5;
	}
	return 0;

}
